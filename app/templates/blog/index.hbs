{{page-title "Blog Index"}}
<b>Blog Index</b>

<LinkTo
    @route="blog.edit"
    @model={{this.model.id}}
    class="btn btn-primary edit_button"
    data-test-button="edit"
>
    <FaIcon @icon="pencil-alt" />
</LinkTo>

{{outlet}}